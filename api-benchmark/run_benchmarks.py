import subprocess; import sys; def run_benchmarks(url): print(f"Benchmarking {url}"); print("=== Latency Test ==="); subprocess.run([sys.executable, "latency_test.py", url]); print("=== Throughput Tests ==="); [subprocess.run([sys.executable, "throughput_test.py", url, str(c), "30"]) for c in [1, 5, 10, 20]]; print("Benchmarks complete"); run_benchmarks(sys.argv[1]) if len(sys.argv) > 1 else print("Usage: python run_benchmarks.py <url>")
