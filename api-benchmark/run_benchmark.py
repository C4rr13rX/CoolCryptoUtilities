import subprocess; import sys; url = sys.argv[1] if len(sys.argv) > 1 else "https://httpbin.org/get"; print(f"Benchmarking {url}"); print("=== LATENCY TEST ==="); subprocess.run([sys.executable, "latency_test.py", url]); print("\n=== THROUGHPUT TEST ==="); subprocess.run([sys.executable, "throughput_test.py", url])
