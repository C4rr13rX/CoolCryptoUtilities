<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- dataSync: the honest classification for what these do — they move
         trading data and sync the wizard node, not play media or track
         location. Android 14+ requires the specific subtype permission. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Long-running work must survive a reboot; the receiver below restarts
         whatever the user had enabled. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".DashboardApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Dashboard">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The Django/Vue server. Foreground because the WebView is useless
             the moment it is killed, and Android will kill a background
             process holding a socket. -->
        <service
            android:name=".services.DjangoService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- The Rust wizard node (native binary, arm64). Separate service so
             it can be restarted or updated without bouncing Django. -->
        <service
            android:name=".services.WizardNodeService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Scheduled Lambda invocations: guardian checks, the trading
             pipeline, the weekly bootstrap. A JobService, not a foreground
             service with a loop — the OS batches these wakeups with every
             other app's and defers them under Doze, which is what keeps a
             three-hourly job from costing battery all day. -->
        <service
            android:name=".services.ScheduledJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
