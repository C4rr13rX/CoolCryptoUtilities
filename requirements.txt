# Crypto & security
cryptography>=3.4.8
pycryptodome>=3.15.0
kyber-py>=1.0.1

# TUI / CLI
textual>=7.5.0
rich>=13.0.0

# Networking & web scraping
requests>=2.31.0
aiohttp>=3.9.0
async-timeout>=4.0.3
beautifulsoup4>=4.12.2
feedparser>=6.0.11

# Blockchain
web3>=6.14.0
eth-account>=0.10.0
hexbytes>=0.3.1

# Data & ML
pandas>=2.1.0; python_version>="3.9"
pandas>=1.5.3; python_version<"3.9"
numpy>=1.24.0
# !=2.21.0: its cp313 Windows wheel ships a python312-linked TFLite metrics
# pyd (ImportError: _pywrap_tensorflow_lite_metrics_wrapper). 2.20.0 is good.
tensorflow>=2.14.0,!=2.21.0
matplotlib>=3.7.0
sympy>=1.12

# Image processing
Pillow>=10.0.0

# Graph database
kuzu>=0.4.0; python_version>="3.9"

# Django stack
Django>=4.2
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
channels>=4.0.0
psycopg[binary]>=3.1.0
# Production WSGI server — launch_revenir.ps1 uses this instead of
# manage.py runserver so the panel survives long-running use.
waitress>=3.0
# Static-file serving for waitress — Django's runserver auto-serves /static/
# but pure WSGI servers don't, so whitenoise middleware handles it.
whitenoise>=6.6

# AWS
boto3>=1.28.0

# AI providers
openai>=1.0.0

# Environment & config
python-dotenv>=1.0.0

# System monitoring
psutil>=5.9.0

# Testing
pytest>=7.4.0
pytest-django>=4.8,<5
openpyxl>=3.1,<4
reportlab>=4.2,<5
pypdf>=5,<7
pyarrow>=16,<24
